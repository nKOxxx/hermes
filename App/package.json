{
  "name": "ares-agent",
  "version": "3.0.0",
  "description": "Personal multi-agent orchestrator — parallel Claude, GPT & MiniMax agents in isolated git worktrees",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dev": "NODE_ENV=development concurrently \"cd frontend && npm run dev\" \"electron .\"",
    "build": "cd frontend && npm run build",
    "backend": "node backend/server.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0",
    "cors": "^2.8.5",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "electron": "^33.0.0"
  }
}
